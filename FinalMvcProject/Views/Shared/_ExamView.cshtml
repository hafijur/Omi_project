@model List<FinalMvcProject.Models.Question>

@if (Model != null)
{
    
for (int i = 0; i < Model.Count()-1; i++)
{
    var first = "same" + i;
    var second = "same" + i;
    var third = "same" + i;
    var forth = "same" + i;
    var number = i + 1;
     <h1>@number. @Model[i].QuestionFor </h1>
    <input name="@first" type="radio" value="@Model[i].First" />
     @Model[i].First
    <input name="@second" type="radio" value="@Model[i].Second" />
     @Model[i].Second
    <input name="@third" type="radio" value="@Model[i].Third" />
     @Model[i].Third
     <input name="@forth" type="radio" value="@Model[i].Forth" />
     @Model[i].Forth
 }
}